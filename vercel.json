{
  "installCommand": "git submodule update --init --recursive && npm install",
  "buildCommand": "rm -rf public/public_articles && mkdir -p public && cp -r public_articles public/ && npm run build:vercel && npm run generate:rss",
  "rewrites": [
    {
      "source": "/tags/:tag",
      "destination": "/tags/:tag.html"
    }
  ]
}